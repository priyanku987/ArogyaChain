module.exports.enrollUser = async (req, res) => {
  try {
    // 1. Implement a way to check if the user is already being enrolled
    // 2. Enroll the user with cerificate authority
  } catch (error) {
    console.log(error);
  }
};
